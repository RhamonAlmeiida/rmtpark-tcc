import { FuncionariosService } from './funcionarios-service';

describe('FuncionariosSerice', () => {
  it('should create an instance', () => {
    expect(new FuncionariosService()).toBeTruthy();
  });
});
