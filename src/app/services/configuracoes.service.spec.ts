import { ConfiguracoesService } from './configuracoes.service';

describe('ConfiguracoesService', () => {
  it('should create an instance', () => {
    expect(new ConfiguracoesService()).toBeTruthy();
  });
});
